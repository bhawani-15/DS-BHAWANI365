#include<stdio.h>
int main(){
    int arr[100];
    int N;
    printf("Enter the number of elements in an array:");
    scanf("%d",&N);
    printf("Enter the array:"):
    for ( i = 0; i < N; i++)
    {
       scanf("%d",&arr[i]);
    }
    
    return 0;
}